This `docs/` folder exists so GitHub Pages builds have a valid source directory.

- `index.html` is the published dashboard page.
- `.nojekyll` is present to bypass Jekyll processing for static assets when needed.
